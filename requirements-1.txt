# Core pipeline (required for: python store_sales_forecasting.py)
pandas>=1.5
numpy>=1.23
matplotlib>=3.6
seaborn>=0.12
scikit-learn>=1.2
xgboost>=1.7

# Optional: only needed for the interactive dashboard
# (streamlit run store_sales_forecasting.py). The CLI pipeline works fine
# without this package installed.
streamlit>=1.30
